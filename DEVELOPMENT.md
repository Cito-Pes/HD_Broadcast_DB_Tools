# 방송 DB 처리 시스템 개발 문서

## 1. 문서 목적

이 문서는 HD Broadcast DB Tools 프로젝트의 기능, 구성, 실행 흐름, DB 작업 구조를 기록한다.

작업 방식과 공통 개발 규칙은 `AGENTS.md`를 따른다.

## 2. 주요 기능

### 2.1 전화번호 중복 정보 추출

`broadcast_db_tool.py`의 첫 번째 탭에서 전화번호를 입력받아 다음 정보를 조회한다.

- 전체 방송 중복 건수
- 전체 방송 최근 60일 중복 건수
- 선택한 방송사의 최근 60일 중복 건수
- 전체 방송 최근 60일 상담사명
- 선택한 방송사 최근 60일 상담사명

조회 결과는 화면의 통합 테이블에 표시하고 `.xlsx` 파일로 저장할 수 있다.

전화번호 입력 시 숫자 이외의 문자를 제거하고 중복 전화번호를 제거한다.

### 2.2 상담내역 등록

두 번째 탭에서 회원코드, 상담구분, 상담내용을 입력한 뒤 여러 회원코드에 상담내역을 일괄 등록한다.

- 회원코드는 줄 단위로 입력한다.
- 빈 값과 중복 회원코드는 제거한다.
- 등록 과정은 로그로 표시한다.
- 회원코드별 성공·실패 건수를 표시한다.

## 3. 주요 파일

| 파일 | 역할 |
|---|---|
| `broadcast_db_tool.py` | 메인 GUI, 조회 쿼리, 상담 등록, Excel 출력 |
| `DB_conn.py` | 설정 파일 처리, DB 설정 로드, SQL Server 연결 |
| `employee_login.py` | 사원 로그인 UI와 인증 |
| `config.txt` | 조회 제외 상담사·조직 등 운영 설정 |
| `requirements.txt` | Python 의존성 목록 |
| `build_exe.py` | 실행 파일 빌드 설정 |
| `*.spec` | PyInstaller 패키징 설정 |
| `img/` | 실행 파일 및 UI 관련 이미지 리소스 |

## 4. 실행 흐름

1. `QApplication`을 생성한다.
2. `Config_DB.db` 존재 여부를 확인한다.
3. 파일이 없으면 `download_db()`로 설정 DB를 다운로드한다.
4. `load_db_config()`으로 SQL Server 접속 설정을 로드한다.
5. `LoginDialog`에서 사원 로그인을 수행한다.
6. 로그인 성공 시 `MainWindow`를 표시한다.
7. 방송사 목록과 상담구분 목록을 워커 스레드로 로드한다.
8. 사용자의 조회 또는 상담 등록 요청을 워커 스레드에서 처리한다.

## 5. DB 연결 구조

DB 연결은 `broadcast_db_tool.py`에서 직접 연결 문자열을 만들지 않고 `DB_conn.py`의 공통 함수를 사용한다.

- 설정 로드: `load_db_config()`
- SQL Server 연결: `get_mssql_connection(db_config)`
- 설정 DB 다운로드: `download_db()`

주요 DB 작업 클래스는 다음과 같다.

- `ChannelLoadWorker`: 방송사 목록 조회
- `ConsultTypeWorker`: 상담구분 목록 조회
- `QueryWorker`: 중복 및 상담사 조회
- `MemoInsertWorker`: 상담내역 INSERT

## 6. 조회 데이터 처리 흐름

`QueryWorker`가 다섯 개의 조회를 실행한 뒤 결과를 `dict`로 반환한다.

`build_integrated_data()`는 전화번호별로 조회 결과를 합쳐 화면과 Excel 출력에서 공통으로 사용할 목록을 만든다.

중복 건수는 조회된 기존 건수에 입력 전화번호 자체를 의미하는 `+1` 보정을 적용한다.

## 7. Excel 출력

`export_to_excel()`이 `xlsxwriter`로 `중복데이터` 시트를 생성한다.

출력 열은 다음 순서다.

1. 전화번호
2. 전체방송중복
3. 전체방송 60일중복
4. 선택 방송사 60일중복
5. 전체 60일 상담사명
6. 선택 방송사 60일 상담사명

## 8. 유지보수 시 확인할 부분

- `config.txt`의 제외 상담사·조직 값이 비어 있을 때 `NOT IN` SQL이 정상 생성되는지 확인한다.
- SQL 문자열에 포함되는 방송사와 설정값은 입력 검증 및 파라미터 처리를 검토한다.
- `QUERY_INSERT_MEMO`의 `consult_type`과 실제 DB 컬럼 매핑을 확인한다.
- 상담내역 대량 등록 시 회원코드별 즉시 `commit()`이 성능에 미치는 영향을 확인한다.
- DB 연결과 커서가 예외 상황에서도 정상적으로 닫히는지 확인한다.
- 조회 결과에서 가져오지만 현재 화면에 사용하지 않는 `AssignDate`, `MemberNo`, `Charge_IDP`의 활용 여부를 확인한다.

## 9. 변경 후 기본 검증

- Python 문법 검사
- Git diff 확인
- 화면 동작 확인
- DB 조회 쿼리의 입력값 및 빈 목록 처리 확인
- Excel 파일 생성 및 열 순서 확인

실제 DB 등록 테스트는 테스트 DB 또는 사용자가 지정한 안전한 대상에서만 수행한다.




