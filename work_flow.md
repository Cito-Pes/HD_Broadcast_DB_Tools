
## 업무내용
1. 엑셀파일 조회
2. 중복DB체크
3. 이벤트 고객 선정
4. 배정상담사
5. 배정구분 정리
6. DB현황보고
 
 ## 기본사항
 1. 파일을 불러온 경로에 파일들을 생성한다.


 ## 1. 엑셀파일 조회
 1. 메인윈도우에서 종속되는 새로운 창을 띄워 작업을 진행한다.
 2. 방송사 선택 콤보박스에서 방송사를 선택 후 파일불러오기 버튼으로 파일검색 다이얼로그 호출. 
 3. 방송사에서 보내준 엑셀파일 (xlsx 또는 xls)을 불러온다. (방송사마다 엑셀파일의 모양은 다르다.)
 4. 임시테이블을 생성하고, 불러온 엑셀파일을 테이블에 불러온다.
 5. "파일정리" 버튼을 눌러 파일내용을 DB로 사용 할 수 있도록 정리한다.
 - DB로 사용할 부분을 제외한 해더 위쪽에 불필요한 부분은 제거. (방송사마다 해더의 위치가 다름)
 - 테스트, 취소, 철회등 DB로 사용 못하는 데이터는 제거한다.
 6. "DB 배정준비" 버튼을 눌러 "파일정리"로 정리된 테이블중 필요한 컬럼만 추출한다.
 - 방송사마다 제고하는 컬럼의 내용이 다르므로 추출가능한 컬럼도 상이하다. 위치와 명칭도 제각가
 - 이 화면에서 최종 생성될 컬럼명은 `순번`, `주문번호`,`주문접수일`,`고객번호`,`고객명`,`상담전화번호`,`상품명`,`출하지시수량`,`고객메세지`,`방송사`,`소채널`
 - 방송사의 엑셀 컬럼과 생성될 컬럼의 매칭은 따로 정의하기로 한다.
 - 추출한 데이터를 데이터프레임으로만들어 메인윈도우로 보내고 창을 닫는다.

 ## 2. 중복DB체크
 1. 앞에서 파일을 동일한 형식으로 정리된 `df`를 받았다.
 2. `방송DB` 테이블을 생성하고 받은 `df`를 입력한다. 테이블에 추가 컬럼을 생성한다. 
 - `전체방송중복`, `전체방송 60일중복`, `방송사 60일중복`, `전체방송 60일 상담사명`, `방송사 60일 상담사명`, `이벤트`, `배정구분`, `상담사`, `상담사`, `배정일`, `배정차수` 컬럼 추가
 - 최종 컬럼은 `순번`, `주문번호`,`주문접수일`,`고객번호`,`고객명`,`상담전화번호`,`상품명`,`출하지시수량`,`고객메세지`,`방송사`,`소채널`,`전체방송중복`, `전체방송 60일중복`, `방송사 60일중복`, `전체방송 60일 상담사명`, `방송사 60일 상담사명`, `이벤트`, `배정구분`, `상담사`, `사번`, `배정일`, `배정차수`
 - 추가된 컬럼의 배경색은 기존의 배경색과 구분되도록 한다.
 - 총 데이터수를 `방송DB 수` 로 기억한다.
 3. 고객이 `고객메세지`에서 상담전화번호 변경을 요청한 경우 `상담전화번호`를 그에 맞춰 변경한다. 
 4. `상담번화번호`의 중복을 제거한다. 같은 `상담전화번호` 중 최초(`순번`이 가장 빠른)의것만 유지하고 나머지 제거한다.
 - 중복된 데이터 수를 `중복DB 수`로 기억한다.
 4. `broadcast_db_tool.py`과 같은 로직으로 `전체방송중복`, `전체방송 60일중복`, `방송사 60일중복`, `전체방송 60일 상담사명`, `방송사 60일 상담사명` 데이터를 입력한다.
 
 ## 3. 이벤트 고객 선정
 1. `이벤트 명칭`과 이벤트에 선정된 고객의 `주문번호`를 입력받는다.
 2. 이벤트의 종류가 1개 이상인 경우도 있고, 이벤트당 당첨된 `주문번호`가 1개 이상일수 있다.
 3. 한 방송당 이벤트 종류를 4개까지 입력가능하도록 한다. 4개의 이벤트는 이벤트1, 이벤트2, 이벤트3, 이벤트4 탭으로 구성한다.
 4. 이벤트 탭별로 `이벤트 명칭`과 당첨된 `주문번호`들을 입력한다.
 5. 입력되는 `주문번호`는 엔터로 구분되며, 입력된  줄마다 3번째 문자 이후의 공백 또는 탭은 무시한다. (공백 또는 탭 이후에는 고객명 등이 불필요하게 따라오는 경우가 있다.)
 6. "이벤트 당첨 적용" 버튼을 누르면 입력받은 `주문번호`와 `방송DB`테이블의 `주문번호`가 일치하는 고객의 `이벤트` 컬럼에 입력받은 `이벤트 명칭`을 입력하고, `배정구분`은 "이벤트"라고 입력한다.
 7. 이벤트1 탭에 `이벤트 없음` 체크박스. 
 8. `이벤트 없음` 체크박스에 체크가 없이 "이벤트 당첨 적용"클릭이 없으면 다음 진행불가.

## 4. 배정상담사
1. DB를 배정받을 상담사를 입력받는 표.
    - `상담사`,`사번`, `일반`, `중복`, `합계`,`이벤트`, `BO`
        + `합계` =  `일반`+`중복`
    - 쿼리 : ```SELECT SaName, SaBun, BranchOffice FROM Staff WHERE PlaceofDuty in ('홈쇼핑 TM','미채결') and BranchOffice in ('TM1','TM2') and OutDate='' order by BranchOffice, SaName ```
    - 매핑 : `상담사` = SaName, `사번` = SaBun, `BO` = BranchOffice
    - DB배정은 수차례에 걸처서 배정을 해. `배정일`, `배정차수`가 수차례 발생하는데 어떻게 화면을 구성해야할지 막막함.
    - 테이블명은 `배정상담사`, 로딩시 `배정일`은 오늘, `배정차수` 1로 기본값 조회
2. 배정일, 배정차수 추가로 기능.

## 5. 배정구분 정리
1. "배정구분 정리" 버튼 클릭하여 아래내용 진행
2. `방송DB`테이블의 `배정구분`의 값이 비어있는 row만 작업을 진행하면 순차적으로 진행한다.
3. `배정구분` 값이 비어있고, `전체방송중복`의 값이 8 초과이면 `배정구분`은 "블랙리스트", `상담사`는 "블랙리스트"로 입력
4. `배정구분` 값이 비어있고, `전체방송 60일 상담사명`의 값이 `배정상담사`테이블의 `배정차수`가 1이고 합계의 값이 1이상 또는 `BO`값이 'TM1'인 상담사와 일치하면 `배정구분은`은 "본인중복", `상담사`는 일치하는 상담사명 입력
5. `배정구분` 값이 비어있고, `전체방송중복` 값이 1 이면 `배정구분`은 "일반"
6. `배정구분` 값이 비어있고, `전체방송중복` 값이 2 이상이면 `배정구분`은 "중복"

## 6.DB현황보고
1. 보고 현황 값
    - `방송DB` : 2.중복체크 에서 기억한 `방송DB 수`
    - `중복DB` : 2.중복체크 에서 기억한 `중복DB 수`
    - `이벤트` : `방송DB`테이블의 `배정구분`이 `이벤트`인 DB의 수
    - `본인중복` : `방송DB`테이블의 `배정구분`이 `본인중복`인 DB의 수
    - `블랙리스트` : `방송DB`테이블의 `배정구분`이 `블랙리스트`인 DB의 수
    - `일반` : `방송DB`테이블의 `배정구분`이 `일반`인 DB의 수
    - `중복` : `방송DB`테이블의 `배정구분`이 `중복`인 DB의 수
2. 보고서 형식
    |`DB구분`| `갯수`|
    |----|----|
    |`방송DB 수`|  |
    |`중복DB 수`|  |
    |`이벤트`|  | 
    |`본인중복`|  | 
    |`블랙리스트`|  | 
    |`일반`|  | 
    |`중복`|  | 

## 7. DB의 배정
### 1차 배정 (`배정차수` 1 일때)
0. "`배정차수`차 DB배정" 버튼을 누르면 아래의 업무를 진행한다. 버튼의 텍스트는 "1차 DB배정"의 형식
1. `배정상담사` 테이블에 입력된 상담사별 `일반`,`중복`,`이벤트`의 수량만큼 `방송DB`테이블에 일치하는 `배정구분`이 일치하고, `상담사`가 없는 곳에 할당을 한다.
2. 순차적 배정이 아닌 무작위 할당으로 한다. (`방송DB`테이블의 순서를 무작위로 섞은 후 순차 배정하던, 그대로 두고 무작위 배정하던 상관없음. )
3. 할당되지 않은 상담사는 "DB관리자"를 할당한다. (사번 : 20250221)
4. 1차 배정이 완료되면 `방송DB`테이블을 DB에 저장.
5. "배정파일(xls)생성" 버튼을 누르면 `방송DB` 테이블중에 `고객명`, `상담전화번호`, `고객번호`, `주문번호`,`주문접수일`,`상품명`,`출하지시수량`,`고객메세지`,`방송사`,`사번` 항목을 순서대로 엑셀로 생성
    - 엑셀은 `Excel 97 - 2003통합문서 (*.xls)` 형식으로 저장하며, 파일명은 `방송사`_`주문접수일`_DB배정.xls 
6. "문자 발송파일 생성" 버튼을 누르면 `방송DB` 테이블중에 `상담사`가 블랙리스트를 제외하고 `주문번호`를 엑셀파일로 생성.
    - 엑셀은 `Excel 97 - 2003통합문서 (*.xls)` 형식으로 저장하며, 파일명은 `방송사`_`주문접수일`_문자발송.xls 
7. "배포파일 생성" 버튼을 누르면 아래의
    - `방송DB` 테이블의 항목`주문접수이`,`고객명`,`상담전화번호`,`주문번호`,`전체방송중복`, `전체방송 60일중복`, `방송사 60일중복`, `전체방송 60일 상담사명`, `방송사 60일 상담사명`,`상담사`,`이벤트`,`출하지시수량`,`고객메세지`,`배정일`,`배정차수`를 [text](reprance/배포파일/20260726_쇼핑엔티_가미숙.xlsx) 형식으로 엑셀파일을 생성합니다.
    - 상담사별로 파일을 생성.
    - 파일명은 `배정일`_`방송사`_`상담사`_`배정차수`.xlsx 
    - 배포_`배정차수` 이름으로 폴더를 만들고 그 안에 넣는다.


### 2차 ~ n차 배정
1. 이전에 배정이후 다른 경로를 통해서 배정상황이 변경되는 경우가 발행을 해. 실제 사용 DB와 `방송DB`테이블에서 저장한 DB를 교차검증을 진행후 진행하는게 업무적인 오류가 없어.
    - 실제 DB는 TM_MEMBER 테이블이고, TM_MEMBER의 OrderNo의 값과 `방송DB`의 `주문번호`와 일치해. 
    - TM_MEMBER의 AssignCharge_ID와 `방송DB`의 `사번`이 일치하지 않으면, `사번`을 AssignCharge_ID로 업데이트 하고, `상담사`도 수정을 한다. `배정일`도 AssignDate 과 동일하게 수정
2. 데이터 보정완료 확인 메시지
3. "`배정차수`차 DB배정" 버튼을 누르면 아래의 업무를 진행한다. 버튼의 텍스트는 "2차 DB배정"의 형식
4. 상위 4번 배정상담사의 