# HD Broadcast DB Tools 작업 지침

## 기본 원칙

- 항상 실제 확인한 내용만 보고한다.
- 확인하지 않은 동작이나 테스트 결과를 완료된 것으로 말하지 않는다.
- 기존 사용자 변경사항과 기존 동작을 임의로 삭제하거나 덮어쓰지 않는다.
- 변경 범위를 요청된 작업에 한정한다.
- 답변은 한국어 반말로 간결하게 작성한다.

## 프로젝트 개요

- 운영체제: Windows
- 언어: Python
- GUI: PySide6
- 데이터베이스: SQL Server
- Excel 출력: `xlsxwriter`
- 주요 실행 파일: `broadcast_db_tool.py`

## DB 연결 규칙

- DB 연결은 반드시 `DB_conn.py`의 공통 함수를 우선 사용한다.
- `broadcast_db_tool.py`에서 직접 연결 문자열이나 인증 정보를 새로 만들지 않는다.
- DB 설정은 `load_db_config()`을 사용해 로드한다.
- SQL Server 연결은 `get_mssql_connection(db_config)`을 사용한다.
- `Config_DB.db`가 없을 때는 기존 `download_db()` 흐름을 따른다.
- 인증 정보, 연결 문자열, Google Drive 설정값을 소스 코드에 하드코딩하지 않는다.
- DB 변경 쿼리는 실행 전 대상 테이블과 조건을 확인한다.
- 대량 INSERT/UPDATE/DELETE를 추가할 때는 트랜잭션 처리와 실패 시 롤백 여부를 명확히 한다.

## 코드 수정 규칙

- `broadcast_db_tool.py`의 GUI는 메인 스레드를 차단하지 않도록 DB 작업을 `QThread` 또는 기존 워커 구조에서 처리한다.
- 기존 `ChannelLoadWorker`, `ConsultTypeWorker`, `QueryWorker`, `MemoInsertWorker` 구조를 우선 재사용한다.
- SQL 문자열을 직접 조합할 때는 입력값 검증을 먼저 하고, 가능한 경우 파라미터 바인딩을 사용한다.
- SQL의 `IN` 또는 `NOT IN` 목록이 비어 있을 때 잘못된 SQL이 생성되지 않도록 처리한다.
- DB 연결과 커서는 예외가 발생해도 정리되도록 작성한다.
- 기존 UI 동작, 컬럼 순서, Excel 헤더를 변경할 때는 영향 범위를 확인한다.
- 기능 변경 시 관련 주석과 문서도 함께 갱신한다.

## 검증 규칙

변경 후 가능한 범위에서 다음을 확인한다.

1. Python 문법 검사
2. 변경된 함수의 호출 흐름 확인
3. SQL 파라미터와 컬럼 매핑 확인
4. Excel 출력 형식 확인
5. Git diff와 작업 트리 확인

DB에 실제 데이터를 쓰는 테스트는 사용자가 명시적으로 요청하거나 안전한 테스트 DB가 확인된 경우에만 수행한다.

## Git 규칙

- 작업 전에 `git status`로 기존 변경사항을 확인한다.
- 사용자가 요청하지 않은 파일은 수정하지 않는다.
- 커밋은 사용자가 명시적으로 요청한 경우에만 생성한다.
- 커밋 전에는 `git diff`로 변경 내용을 확인한다.
- 비밀번호, 토큰, DB 파일, 개인 설정 파일을 커밋하지 않는다.

## 보고 규칙

- 수정한 파일과 핵심 변경 내용을 명확히 보고한다.
- 실행한 검증과 결과를 구분해서 보고한다.
- 실패하거나 확인하지 못한 항목은 그대로 밝힌다.
- DB 접속·조회·등록 작업은 실제 실행 여부를 명시한다.
